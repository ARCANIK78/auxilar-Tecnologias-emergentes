let n = 8;
let r = (n > 0) ? 'Es positivo' : 'No es positivo';
console.log(r);

let letra = 'A';
let rr = (letra === 'a' || letra === 'A') ? 'Es vodal':
         (letra === 'e' || letra === 'E') ? 'Es vodal':
         "No es vodal"
console.log(rr)